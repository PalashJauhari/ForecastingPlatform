# GaussianBlurr — models, middleware tuning, and workspace paths

models:
  orchestrator: "gpt-4o-mini"
  code_generation: "gpt-4o-mini"
  code_judge: "gpt-4o-mini"
  summarization: "gpt-4o-mini"

middleware:
  context_editing:
    keep_recent_messages: 10
  summarization:
    token_threshold: 100000
  tool_call_limit:
    max_calls: 25

code_pipeline:
  timeout_seconds: 120

skills:
  max_skill_context_tokens: 3000

paths:
  # Logical prefix AND on-disk folder at repo root: ./agent_filesystem/<session>/input|output/
  agent_filesystem: "agent_filesystem"
