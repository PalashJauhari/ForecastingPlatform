# Environment — loads project-root `.env` into the process (OPENAI_API_KEY, optional Langfuse, etc.)
# Used by `api/main.py` and `graph/graph.py` before LLM clients are constructed.
python-dotenv>=1.0.0

# Typing — `graph/graph.py` uses `typing_extensions.TypedDict` for `AgentState`
typing-extensions>=4.8.0

# LangChain + LangGraph — custom StateGraph agent (`graph/graph.py`), ToolNode, checkpoints
# LangChain 1.x (incl. ``ToolRuntime`` used by tools/) requires Python 3.10+. On 3.9, install stays on 0.3.x — recreate the venv with ``python3.10+`` for a full match to this repo.
langchain>=1.0.0; python_version >= "3.10"
langchain>=0.3.28,<1; python_version < "3.10"
# ``langchain_core.rate_limiters.InMemoryRateLimiter`` (``ChatOpenAI(..., rate_limiter=...)``) lives in langchain-core.
langchain-core>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.3.0
langgraph-checkpoint>=2.0.0
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0

# API
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0

# UI (Plotly Dash)
dash>=2.14.0
requests>=2.28.0

# Data
pandas>=2.0.0
openpyxl>=3.1.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pyyaml>=6.0

# Code safety scanning (`code_pipeline` Semgrep gate)
semgrep>=1.50.0

# Observability — ``langfuse`` 4.x (``propagate_attributes``, etc.) needs Python 3.10+; 3.9 stays on v3
langfuse>=4.0.0; python_version >= "3.10"
langfuse>=3.0.0,<4; python_version < "3.10"
